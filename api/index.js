// /api/index.js

import { GoogleGenerativeAI } from '@google/generative-ai';

export const config = {
    api: { bodyParser: true },
};

// --- Вспомогательная функция 1: Форматирование приемов пищи ---
function formatMeals(meals) {
    if (!meals || Object.keys(meals).length === 0) {
        return 'Приемы пищи не добавлены.';
    }
    let mealSummary = '';
    for (const mealKey in meals) {
        const meal = meals[mealKey];
        mealSummary += `\n**${meal.name}:**\n`;
        if (meal.items && meal.items.length > 0) {
            meal.items.forEach(item => {
                // Округление для красивого вывода
                mealSummary += `- ${item.name} (${Math.round(item.grams)}г): К:${Math.round(item.calories)}, Б:${Math.round(item.proteins)}, Ж:${Math.round(item.fats)}, У:${Math.round(item.carbs)}\n`;
            });
        } else {
            mealSummary += `- Продукты не добавлены\n`;
        }
    }
    return mealSummary;
}

// --- Вспомогательная функция 2: Сборка ПОЛНОГО промпта ---
function buildAnalysisPrompt(userData) {
    const { goal, dailyFact, dailyTarget, glHistory = {}, meals } = userData;
    const formattedMeals = formatMeals(meals);

    const goalNames = {
        'lose': 'Снижение веса', 'maintain': 'Поддержание формы', 'gain': 'Набор массы',
        'athlete': 'Профессиональный спорт (Атлет)', 'diabetes1': 'Сахарный диабет 1 типа', 'diabetes2': 'Сахарный диабет 2 типа / ИР',
        'no_gallbladder': 'Отсутствие желчного пузыря', 'gallstones': 'Желчнокаменная болезнь',
        'gi_issues': 'Проблемы с ЖКТ', 'pregnancy': 'Беременность / Лактация',
        'app': 'Антипаразитарная программа (АПП)', 'anticandida': 'Антикандидный протокол',
        'keto_lchf': 'Кето / LCHF', 'vegan_veg': 'Вегетарианство / Веганство'
    };
    const userGoalName = goalNames[goal] || goal;

    return `[[1. РОЛЬ И СТИЛЬ]
Ты - Андрей Солдатенко, нутрициолог (20 лет опыта), основатель Best&People.
* **Стиль:** Наставник-партнер. Тон уважительный, поддерживающий, но экспертный. **ОБЯЗАТЕЛЬНОЕ ОБРАЩЕНИЕ К ПОЛЬЗОВАТЕЛЮ НА "ВЫ"**.
* **Философия:** "Обычная еда — это база, но она часто 'пустая'. Продукты Best&People — это 'умная надстройка' (по-настоящему полноценная еда), фундамент здоровья".

[2. ВХОДНЫЕ ДАННЫЕ]
* **Цель:** ${userGoalName}
* **ФАКТ:** К:${dailyFact.calories}, Б:${dailyFact.proteins}, Ж:${dailyFact.fats}, У:${dailyFact.carbs}, Клетчатка:${dailyFact.fiber}, ГН:${dailyFact.gl}, Вода:${dailyFact.water} мл.
* **ЦЕЛЬ:** К:${dailyTarget.calories}, Б:${dailyTarget.proteins}, Ж:${dailyTarget.fats}, У:${dailyTarget.carbs}, Клетчатка:${dailyTarget.fiber}, Вода:${dailyTarget.water} мл.
* **Рацион:**
${formattedMeals}

[3. БАЗА ЗНАНИЙ BEST&PEOPLE]
* **Натощак:** Aloe Vera Smart Food.
* **Еда:** Shape Smart Food (коктейли, изолят белка), Detox Gentle (Умная Каша), Пюре Re:Balance (Супы/Пюре, изолят белка).
* **Напитки:** Herbal Mix, Coffee Flat White, It's Fiber, Drain.

[4. АЛГОРИТМ АНАЛИЗА (СТРОГОЕ ИСПОЛНЕНИЕ)]

**СЦЕНАРИЙ А: ЭКСТРЕННЫЙ (Если Факт Калорий < 500)**
ЕСЛИ калорий меньше 500, ИГНОРИРУЙ все остальные инструкции и выдай ТОЛЬКО этот текст (без лишних вступлений):
"# Экстренный разбор рациона 🚨
Похоже, Вы не внесли все приемы пищи в дневник, так как калорийность критически низкая (${dailyFact.calories} ккал). Анализ невозможен.

**Если же это голодовка:**
Я, как нутрициолог, обязан предупредить: голодовка — это клеточный стресс, а не очищение. Клетки начинают 'нервничать' и стареть без питания, метаболизм падает.
**Решение:** Не рискуйте. Выпейте коктейль **Shape Smart Food** или съешьте **Пюре Re:Balance**. Это чистый изолят белка с полным аминокислотным профилем (без животных компонентов): он накормит клетки и превратит вредную голодовку в полезную разгрузку, не перегружая ЖКТ."

**СЦЕНАРИЙ Б: ПОЛНЫЙ АНАЛИЗ (Если Факт Калорий >= 500)**
Пиши полный отчет по этой структуре:

**1. Заголовок:** "# Персональный разбор от Андрея Солдатенко 👨‍⚕️"

**2. Вводная часть (Только для сложных протоколов):**
* Если цель Кето, Веган, АПП или Кандида -> СКАЖИ: "Вижу, Вы выбрали сложный протокол. Как эксперт, подчеркиваю: такие диеты — это инструмент. Главное здесь — **без фанатизма**. Наша задача — достичь цели, не допустив дефицитов."

**3. Водный баланс:**
* Сравни Факт (${dailyFact.water}) и Цель (${dailyTarget.water}).
* Если мало -> Объясни риски (нет детокса). Если норма -> Похвали.

**4. Золотой стандарт (Умная проверка):**
* Проверь завтрак (есть ли Aloe + [Shape ИЛИ Detox ИЛИ Пюре]).
* ЕСТЬ? -> ПОХВАЛИ: "Отличное начало дня! Выполнили 'Золотой стандарт', организм получил всё необходимое".
* НЕТ? -> НАПОМНИ: "Идеальное утро начинается с Aloe Vera и Shape Smart Food. Вы можете заменить их **обычной едой** (например, омлет, каша), но это будет менее эффективно для усвоения и контроля веса".

**5. Глубокий анализ для цели "${userGoalName}" (САМОЕ ВАЖНОЕ):**
* **IF 'app' (АПП) OR 'anticandida' (Кандида):**
    * **ОБЯЗАТЕЛЬНО СКАЖИ:** "На этом протоколе питание сильно ограничено. Именно для таких ситуаций я создал кашу **Detox Gentle (Яблочный пирог)**, когда сам проходил антипаразитарную программу. Это мой личный маст-хэв: она дает сытость, энергию и разрешена на протоколе. Рекомендую!"
* **IF 'diabetes1' (СД 1 типа):**
    * Акцент на подсчете углеводов. Упоминай термин **"Хлебная Единица (ХЕ)"**. Напомни, что продукты B&P имеют выверенное кол-во ХЕ, что упрощает жизнь.
* **IF 'diabetes2' (СД 2 типа / ИР):**
    * Твой фокус — **Гликемическая Нагрузка (ГН)**. Если ГН > 60, предупреди о скачках сахара. Рекомендуй *It's Fiber* и *Drain* для контроля глюкозы.
* **IF 'no_gallbladder' OR 'gallstones' (Проблемы с желчным):**
    * Критично: **Дробное питание** (5-6 раз) и **Минимум жиров**. Рекомендуй Shape Smart Food и Пюре как легкие белковые приемы пищи, которые не нагружают печень.
* **IF 'athlete' (Спорт):**
    * Приоритет — белок (2.2 г/кг). Мышцам нужен материал. Рекомендуй *Best Protein* (изолят) и *BCAA* (в составе Shape).
* **IF 'lose' (Похудение):**
    * Ищи углеводы/жиры на ужин. Предложи заменить ужин на Shape Smart Food (сытость без калорий).
* **IF 'gain' (Набор):**
    * Предложи *добавить* прием пищи (Shape/Пюре) *между* едой, а не вместо неё.

**6. Рекомендации (Двойной подход):**
При проблемах давай два решения:
1. Путь "Обычной еды" (Сложно, долго готовить, риск не угадать КБЖУ).
2. Путь "Best&People" (Быстро, эффективно, гарантированный состав).

**Конец:** Не пиши прощание или дисклеймер (он добавится автоматически).
`;
}

// --- ОСНОВНОЙ ОБРАБОТЧИК ЗАПРОСОВ ---
export default async function handler(req, res) {
    // Настройки CORS
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

    if (req.method === 'OPTIONS') {
        res.status(200).end();
        return;
    }

    if (req.method !== 'POST') {
        res.status(405).json({ error: 'Only POST requests allowed' });
        return;
    }

    try {
        const apiKey = process.env.GEMINI_API_KEY;
        if (!apiKey) throw new Error('GEMINI_API_KEY not found');

        const userData = req.body;
        if (!userData || !userData.dailyFact || !userData.dailyTarget) {
            res.status(400).json({ error: 'Invalid user data' });
            return;
        }
        
        // 1. Создаем управляющие тексты для Приветствия
        const goalNames = {
            'lose': 'Снижение веса', 'maintain': 'Поддержание формы', 'gain': 'Набор массы',
            'athlete': 'Профессиональный спорт (Атлет)', 'diabetes1': 'Сахарный диабет 1 типа', 'diabetes2': 'Сахарный диабет 2 типа / ИР',
            'no_gallbladder': 'Отсутствие желчного пузыря', 'gallstones': 'Желчнокаменная болезнь',
            'gi_issues': 'Проблемы с ЖКТ', 'pregnancy': 'Беременность / Лактация',
            'app': 'Антипаразитарная программа (АПП)', 'anticandida': 'Антикандидный протокол',
            'keto_lchf': 'Кето-диета / LCHF', 'vegan_veg': 'Вегетарианство / Веганство'
        };
        const userGoalName = goalNames[userData.goal] || userData.goal;

        // Приветствие (Мандаторное начало)
        const mandatoryOpening = `Привет! Я Андрей Солдатенко, нутрициолог с 20-летним опытом и основатель компании Best&People. Рад помочь Вам разобраться с Вашим рационом, используя мой личный опыт и новые технические возможности искусственного интеллекта. Ваша текущая цель — ${userGoalName}.`;

        // Дисклеймер (Мандаторное окончание)
        const disclaimerBlock = `\n\n---\n\n**ВАЖНОЕ ПРИМЕЧАНИЕ:** Данные рекомендации основаны на анализе предоставленных Вами цифр и моем профессиональном опыте. Они носят информационный характер. Вы принимаете на себя полную ответственность за использование этой информации. Если у Вас имеются хронические заболевания или Вы принимаете лекарственные препараты, перед любыми изменениями в рационе обязательна консультация с Вашим лечащим врачом.`;
        
        // 2. Генерируем только ТЕЛО АНАЛИЗА
        const analysisPrompt = buildAnalysisPrompt(userData);

        const genAI = new GoogleGenerativeAI(apiKey);
        const model = genAI.getGenerativeModel({
            model: "gemini-2.5-flash" 
        });

        // Отправляем тело промпта
        const result = await model.generateContent(analysisPrompt);
        const text = result.response.text().trim();

        // Если сработал Сценарий А (Голодовка), нейросеть может сама вернуть заголовок. 
        // Проверим, не вернула ли она уже заголовок, чтобы не дублировать.
        let finalText = text;
        
        // 3. Собираем ФИНАЛЬНЫЙ ответ (Приветствие + Анализ + Дисклеймер)
        // ВАЖНО: Если это голодовка (Сценарий А), приветствие все равно уместно, 
        // так как оно задает тон.
        const fullText = mandatoryOpening + "\n\n" + finalText + disclaimerBlock;

        res.status(200).json({ text: fullText });

    } catch (error) {
        console.error('Vercel Function Error:', error.message);
        // В случае ошибки возвращаем 500 и сообщение об ошибке
        res.status(500).json({ error: 'Generation failed: ' + error.message });
    }
}
