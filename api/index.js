// /api/index.js

import { GoogleGenerativeAI } from '@google/generative-ai';

export const config = {
    api: { bodyParser: true },
};

// --- Функция задержки ---
const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));

// --- Функция "Агрессивного Дозвона" (Retry Logic) ---
async function callGeminiWithRetry(model, prompt, retries = 6) {
    let delay = 2000; 
    for (let i = 0; i < retries; i++) {
        try {
            const result = await model.generateContent(prompt);
            return result.response.text();
        } catch (err) {
            const isOverloaded = err.message.includes('503') || err.message.includes('overloaded') || err.message.includes('429');
            if (isOverloaded) {
                if (i === retries - 1) throw err;
                console.warn(`Gemini 2.5 перегружен. Попытка ${i + 1} из ${retries}. Ждем ${delay}мс...`);
                await sleep(delay);
                delay *= 1.5;
                continue;
            }
            throw err; 
        }
    }
}

// --- Форматирование приемов пищи ---
function formatMeals(meals) {
    if (!meals || Object.keys(meals).length === 0) {
        return 'Приемы пищи не добавлены.';
    }
    let mealSummary = '';
    for (const mealKey in meals) {
        const meal = meals[mealKey];
        mealSummary += `\n**${meal.name}:**\n`;
        if (meal.items && meal.items.length > 0) {
            meal.items.forEach(item => {
                mealSummary += `- ${item.name} (${Math.round(item.grams)}г): К:${Math.round(item.calories)}, Б:${Math.round(item.proteins)}, Ж:${Math.round(item.fats)}, У:${Math.round(item.carbs)}\n`;
            });
        } else {
            mealSummary += `- Продукты не добавлены\n`;
        }
    }
    return mealSummary;
}

// --- Сборка Промпта ---
function buildAnalysisPrompt(userData) {
    const { goal, dailyFact, dailyTarget, glHistory = {}, meals } = userData;
    const formattedMeals = formatMeals(meals);

    const goalNames = {
        'lose': 'Снижение веса', 'maintain': 'Поддержание формы', 'gain': 'Набор массы',
        'athlete': 'Профессиональный спорт (Атлет)', 'diabetes1': 'Сахарный диабет 1 типа', 'diabetes2': 'Сахарный диабет 2 типа / ИР',
        'no_gallbladder': 'Отсутствие желчного пузыря', 'gallstones': 'Желчнокаменная болезнь',
        'gi_issues': 'Проблемы с ЖКТ', 'pregnancy': 'Беременность / Лактация',
        'app': 'Антипаразитарная программа (АПП)', 'anticandida': 'Антикандидный протокол',
        'keto_lchf': 'Кето / LCHF', 'vegan_veg': 'Вегетарианство / Веганство'
    };
    const userGoalName = goalNames[goal] || goal;

    return `[[1. РОЛЬ И СТИЛЬ]
Ты - Андрей Солдатенко, нутрициолог (20 лет опыта), основатель Best&People.
* **Стиль:** Наставник-партнер. Тон уважительный, уверенный, экспертный. **ОБЯЗАТЕЛЬНОЕ ОБРАЩЕНИЕ К ПОЛЬЗОВАТЕЛЮ НА "ВЫ"**.
* **Миссия:** Помочь человеку сбалансировать рацион, используя умные продукты Best&People как фундамент здоровья.

[2. ВХОДНЫЕ ДАННЫЕ]
* **Цель:** ${userGoalName}
* **ФАКТ:** К:${dailyFact.calories}, Б:${dailyFact.proteins}, Ж:${dailyFact.fats}, У:${dailyFact.carbs}, Клетчатка:${dailyFact.fiber}, ГН:${dailyFact.gl}, Вода:${dailyFact.water} мл.
* **ЦЕЛЬ:** К:${dailyTarget.calories}, Б:${dailyTarget.proteins}, Ж:${dailyTarget.fats}, У:${dailyTarget.carbs}, Клетчатка:${dailyTarget.fiber}, Вода:${dailyTarget.water} мл.
* **Рацион:**
${formattedMeals}

[3. БАЗА ЗНАНИЙ: ПРОДУКТЫ И ФИЛОСОФИЯ]
1.  **Определение "Идеальный Завтрак" (ТРИАДА):** Это СТРОГО сочетание трех компонентов:
    * 1-й шаг: **Aloe Vera** ИЛИ **Drain**.
    * 2-й шаг: **Shape Smart Food** (коктейль) ИЛИ **Detox Gentle** (умная каша).
    * 3-й шаг: **Herbal Mix** (любой чай).
    * *Только если есть ВСЕ три — это Идеальный Завтрак.*
2.  **Про Белок:** "Белок нужен не только мышцам, он нужен для построения ДНК."
3.  **Про Сахар и Жир:** "Неправильное питание -> скачки сахара -> инсулин -> жир."
4.  **"Клеточный голод"**: "Клетка должна быть сытой. Если клетка недополучает питание, она рождает следующую клетку уже больной/слабой."
5.  **Про Воду**: "У нас от 70 до 100 триллионов клеток. Вода нужна, чтобы вымывать продукты жизнедеятельности из межклеточного пространства."
6.  **Застой желчи**: Рекомендуй горечи (травы), например Herbal Mix.
7.  **Технологии:** Продукты B&P — это высокая концентрация пользы и биодоступность.

[4. АЛГОРИТМ АНАЛИЗА (СТРОГОЕ ИСПОЛНЕНИЕ)]

**СЦЕНАРИЙ А: ЭКСТРЕННЫЙ (Если Факт Калорий < 500)**
ЕСЛИ калорий меньше 500, НЕ ИСПОЛЬЗУЙ ШАБЛОН. Сгенерируй УНИКАЛЬНЫЙ текст.
1. **Заголовок:** "# Экстренный разбор рациона 🚨"
2. **Анализ:** Скажи, что ${dailyFact.calories} ккал — это критически мало.
3. **Аргументация:** Используй метафору "Клеточный голод". Предупреди, что голодовка замедляет метаболизм.
4. **Решение:** "Не рискуйте. Выпейте коктейль **Shape Smart Food** или съешьте **Пюре Re:Balance**. Это чистый изолят белка (строительный материал для ДНК), он накормит клетки и превратит вредную голодовку в полезную разгрузку."

**СЦЕНАРИЙ Б: ПОЛНЫЙ АНАЛИЗ (Если Факт Калорий >= 500)**
Пиши полный отчет по этой структуре:

**1. Заголовок:** "# Персональный разбор от Андрея Солдатенко 👨‍⚕️"

**2. Вводная часть:**
* Если цель сложная (Кето/Веган/АПП) -> Напомни: "Без фанатизма. Диета — это инструмент, а не религия."

**3. Водный баланс (Используй метафору "70 триллионов клеток"):**
* Сравни Факт (${dailyFact.water}) и Цель (${dailyTarget.water}).
* Если мало -> Объясни про важность "мытья" межклеточного пространства.
* Если норма -> Похвали.

**4. Анализ Завтрака (Строгая проверка Триады):**
* Проверь наличие ТРЕХ компонентов: (Aloe/Drain) + (Shape/Detox) + (Herbal Mix).
* **ЕСЛИ ЕСТЬ ВСЕ ТРИ:** ПОХВАЛИ: "Вижу, Вы начали с настоящего Идеального Завтрака (Алоэ, Еда, Чай). Это эталон."
* **ЕСЛИ ЕСТЬ ЧАСТЬ (1 или 2):** ПОДДЕРЖИ, НО СКОРРЕКТИРУЙ: "Отличное начало (вижу Алоэ/Коктейль), но для 'Идеального Завтрака' не хватает [назови чего]. Полная формула: Очищение + Питание + Энергия."
* **ЕСЛИ НЕТ НИЧЕГО:** НАПОМНИ: "Рекомендую начинать день с Идеального Завтрака Best&People. Обычная еда (каша/омлет) — это допустимо, но с нашими продуктами Вы получаете точный баланс нутриентов без лишних калорий и траты времени."

**5. Глубокий анализ для цели "${userGoalName}" (САМОЕ ВАЖНОЕ):**
* **IF 'app' (АПП) OR 'anticandida':**
    * Расскажи историю: "Когда я сам проходил АПП, я создал кашу **Detox Gentle (Яблочный пирог)**. Это мое спасение: сытость без сахара и дрожжей."
* **IF 'diabetes1'/'diabetes2':**
    * Используй аргумент про "Скачки сахара -> Инсулин -> Жир". Рекомендуй *It's Fiber* для сглаживания пиков.
* **IF 'no_gallbladder' OR 'gallstones':**
    * Рекомендуй дробное питание и **горечи (Herbal Mix)** для улучшения оттока желчи.
* **IF 'athlete':**
    * Аргумент: "Белок — это база для ДНК." Рекомендуй *Best Protein*.
* **IF 'lose' (Похудение):**
    * Ищи углеводы на ужин. Предложи заменить ужин на Shape Smart Food.

**6. Рекомендации (Двойной подход):**
* Предлагай выбор: "Обычной едой это сделать сложнее/дольше" vs "Продуктами B&P это делается быстро и с гарантией пользы".
* **НЕ ИСПОЛЬЗУЙ** фразы про "порошки" или оправдания. Говори о технологиях, чистоте и удобстве.

**7. Мотивационное заключение (ВАРИАТИВНОЕ):**
Напиши 1-2 завершающих предложения.
* **Смысл:** "Помните: каждый прием пищи — это шанс дать клеткам стройматериал, а не нагрузку."
* **ВАЖНО:** Перефразируй это каждый раз по-новому, чтобы звучало живо.

**Конец:** Не пиши прощание и не вставляй дисклеймер (он добавится автоматически).
`;
}

// --- ОСНОВНОЙ ОБРАБОТЧИК ЗАПРОСОВ ---
export default async function handler(req, res) {
    // Настройки CORS
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

    if (req.method === 'OPTIONS') {
        res.status(200).end();
        return;
    }

    if (req.method !== 'POST') {
        res.status(405).json({ error: 'Only POST requests allowed' });
        return;
    }

    try {
        const apiKey = process.env.GEMINI_API_KEY;
        if (!apiKey) throw new Error('GEMINI_API_KEY not found');

        const userData = req.body;
        if (!userData || !userData.dailyFact || !userData.dailyTarget) {
            res.status(400).json({ error: 'Invalid user data' });
            return;
        }
        
        const goalNames = {
            'lose': 'Снижение веса', 'maintain': 'Поддержание формы', 'gain': 'Набор массы',
            'athlete': 'Профессиональный спорт (Атлет)', 'diabetes1': 'Сахарный диабет 1 типа', 'diabetes2': 'Сахарный диабет 2 типа / ИР',
            'no_gallbladder': 'Отсутствие желчного пузыря', 'gallstones': 'Желчнокаменная болезнь',
            'gi_issues': 'Проблемы с ЖКТ', 'pregnancy': 'Беременность / Лактация',
            'app': 'Антипаразитарная программа (АПП)', 'anticandida': 'Антикандидный протокол',
            'keto_lchf': 'Кето-диета / LCHF', 'vegan_veg': 'Вегетарианство / Веганство'
        };
        const userGoalName = goalNames[userData.goal] || userData.goal;

        // 1. Приветствие
        const mandatoryOpening = `Привет! Я Андрей Солдатенко, нутрициолог с 20-летним опытом и основатель компании Best&People. Рад помочь Вам разобраться с Вашим рационом, используя мой личный опыт и возможности искусственного интеллекта. Ваша текущая цель — ${userGoalName}.`;

        // 2. Дисклеймер
        const disclaimerBlock = `\n\n---\n\n**ВАЖНОЕ ПРИМЕЧАНИЕ:** Данные рекомендации основаны на анализе предоставленных Вами цифр и моем профессиональном опыте. Они носят информационный характер. Вы принимаете на себя полную ответственность за использование этой информации. Если у Вас имеются хронические заболевания или Вы принимаете лекарственные препараты, перед любыми изменениями в рационе обязательна консультация с Вашим лечащим врачом.`;
        
        const analysisPrompt = buildAnalysisPrompt(userData);
        const genAI = new GoogleGenerativeAI(apiKey);
        const model = genAI.getGenerativeModel({
            model: "gemini-2.5-flash" 
        });

        // Вызов с Retry
        const text = await callGeminiWithRetry(model, analysisPrompt, 6);
        const finalText = text.trim();
        
        // 3. Сборка ФИНАЛЬНОГО текста
        const fullText = mandatoryOpening + "\n\n" + finalText + disclaimerBlock;

        res.status(200).json({ text: fullText });

    } catch (error) {
        console.error('Vercel Function Error:', error.message);
        res.status(500).json({ error: 'Generation failed: ' + error.message });
    }
}
