import { GoogleGenerativeAI } from '@google/generative-ai';

export const config = {
    api: { bodyParser: true },
};

// --- 1. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ---

const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));

async function callGeminiWithRetry(model, prompt, retries = 3) {
    let delay = 2000;
    for (let i = 0; i < retries; i++) {
        try {
            const result = await model.generateContent(prompt);
            return result.response.text();
        } catch (err) {
            console.warn(`Gemini 503/Overloaded. Попытка ${i + 1}/${retries}`);
            if (i === retries - 1) throw err;
            await sleep(delay);
            delay *= 1.5;
        }
    }
}

function formatMealsForAnalysis(meals) {
    if (!meals || Object.keys(meals).length === 0) return 'Данные о приемах пищи отсутствуют (голод).';
    let text = '';
    // Определяем порядок сортировки приемов пищи
    const order = ['breakfast', 'snack1', 'lunch', 'snack2', 'dinner', 'snack3'];
    
    // Сортируем ключи, если они есть, или просто перебираем
    const mealKeys = Object.keys(meals).sort((a, b) => {
        return order.indexOf(a) - order.indexOf(b);
    });

    if (mealKeys.length === 0) return 'Приемы пищи не записаны.';

    mealKeys.forEach(key => {
        const m = meals[key];
        text += `[${m.name}]:\n`; // Используем название как заголовок раздела
        if (m.items?.length) {
            m.items.forEach(item => {
                text += ` - ${item.name} (${item.grams}г): Ккал ${item.calories}, Б ${item.proteins}, Ж ${item.fats}, У ${item.carbs}\n`;
            });
        } else {
            text += ` - (Пусто/Пропущено)\n`;
        }
    });
    return text;
}

function getGoalTitle(goalCode) {
    const goals = {
        'lose': 'Снижение веса 📉',
        'maintain': 'Поддержание веса и здоровья ⚖️',
        'gain': 'Набор мышечной массы 💪',
        'diabetes1': 'Контроль диабета 1 типа 🩸',
        'diabetes2': 'Контроль диабета 2 типа / ИР 🩸',
        'no_gallbladder': 'Питание при удаленном желчном 🚑',
        'gallstones': 'Питание при ЖКБ (камни) 💎',
        'gi_issues': 'Восстановление ЖКТ 🌿',
        'app': 'Антипаразитарная программа 🚫',
        'anticandida': 'Антикандидный протокол 🍄'
    };
    return goals[goalCode] || 'Снижение веса';
}

// --- 2. ГЕНЕРАЦИЯ ПРОМПТА (ВЕРСИЯ 5.0 - УМНАЯ) ---
function buildAnalysisPrompt(userData) {
    const { goal, dailyFact, dailyTarget, meals } = userData;
    const goalTitle = getGoalTitle(goal);
    const mealsData = formatMealsForAnalysis(meals);

    const gallbladderCondition = (goal === 'no_gallbladder' || goal === 'gallstones') 
        ? "🚑 **КРИТИЧНО:** У клиента проблемы с желчным. Настаивай на **дробном питании (5-6 раз)**. Пропуски еды опасны (застой желчи)." 
        : "Напомни про важность питания **4-5 раз в день** для разгона метаболизма.";

    return `
Ты — Андрей Солдатенко, опытный нутрициолог (20 лет стажа) и основатель Best&People.
Твой стиль: Экспертный, заботливый, но конкретный. Ты не "продавец", ты — наставник.
Твой инструмент: Markdown (заголовки, **жирный**, списки, эмодзи).

=== ВХОДНЫЕ ДАННЫЕ ===
ЦЕЛЬ: ${goalTitle}
ФАКТ: Ккал ${dailyFact.calories} (Цель ${dailyTarget.calories}), Белки ${dailyFact.proteins} (Цель ${dailyTarget.proteins}), Вода ${dailyFact.water} мл (Цель ${dailyTarget.water}).
МЕНЮ КЛИЕНТА:
${mealsData}

=== ТВОЯ ЛОГИКА АНАЛИЗА (СТРОГО СЛЕДУЙ ЕЙ) ===

1. **Вода (Нюансы):**
   - Чистая вода = Идеал 💧.
   - Травяные чаи, вода с лимоном = Хорошо, считается в баланс.
   - Кофе, черный чай, сладкие соки, супы = Жидкость, но не "душ".
   - Алкоголь, газировка = Токсины и обезвоживание 🛑.
   *Если выпито мало:* Объясни риск отеков и интоксикации.

2. **Анализ приемов пищи (Хронология):**
   - Иди строго: Завтрак -> 2-й Завтрак -> Обед -> Полдник -> Ужин.
   - **ЕСЛИ ПРИЕМ ПИЩИ ПРОПУЩЕН:** Не игнорируй! Напиши: "Вы пропустили перекус". Дай **конкретный микро-совет** (например: "Съешьте 15г тыквенных семечек, 1 грецкий орех или зеленое яблоко").
   - **ЕСЛИ ОБЫЧНАЯ ЕДА (Тяжелая):** Не говори "тратит ресурсы". Скажи: "Это тяжелая пища. Вместо энергии организм тратит силы на переваривание, появляется сонливость. Клетка остается голодной на микроуровне."
   - **ПРОДАЖА ЧЕРЕЗ ПОЛЬЗУ:** Предлагай продукты B&P как *альтернативу* или *дополнение*, объясняя выгоду (усвоение, легкость), а не просто "купите это".

3. **Метод Двух Путей (Обязательно на Ужин или Обед):**
   - Когда критикуешь прием пищи, дай выбор:
   - 🐢 **Путь 1 (Обычная еда):** Дай КОНКРЕТНЫЙ рецепт с граммами. Пример: "Вам нужно съесть 150г отварной трески + 200г свежих огурцов и зелени". Покажи, что это полезно, но требует времени.
   - 🚀 **Путь 2 (Smart Food):** Предложи Shape Smart Food / Пюре Re:Balance. Аргумент: "Точный баланс, 1 минута, легкость для желудка".

=== СТРУКТУРА ОТВЕТА (MARKDOWN) ===

**ВАЖНО: НЕ ПИШИ ПРИВЕТСТВИЕ В НАЧАЛЕ (оно уже есть).**

### 1. 💧 ВОДНЫЙ БАЛАНС
   - Сравни Факт и Цель. Оцени качество жидкостей (отдели воду/травяной чай от кофе/алкоголя).

### 2. 🍽️ ПОШАГОВЫЙ РАЗБОР РАЦИОНА
   *(Иди по порядку: Завтрак, Второй завтрак, Обед, Полдник, Ужин. Не группируй их!)*
   
   **Заголовок (например: ☀️ ЗАВТРАК)**
   - Анализ съеденного. Если обычная еда тяжелая — объясни мягко.
   - Если съел продукт B&P — похвали.
   
   **Заголовок (например: 🍏 ВТОРОЙ ЗАВТРАК)**
   - Если пропущено: "Пропуск замедляет метаболизм. Перекусите хотя бы (15г семечек / фрукт)".
   
   **Заголовок (например: 🥘 ОБЕД)**
   - Анализ. Если тяжелое мясо/жареное — предупреди о нагрузке.
   
   **Заголовок (например: ⏰ ПОЛДНИК: "ЗОЛОТОЙ ЧАС")**
   - Объясни важность питания в 16:00-17:00 (падение сахара). Если пропуск — предложи Detox Gentle или белковый батончик.

   **Заголовок (например: 🌙 УЖИН)**
   - Анализ.
   - **ПРИМЕНИ МЕТОД ДВУХ ПУТЕЙ:**
     - 🐢 Путь обычной еды (КОНКРЕТНЫЕ ГРАММЫ: 150г рыбы, 200г овощей...).
     - 🚀 Путь B&P (название продукта и выгода).

### 3. 🧬 ФИЗИОЛОГИЯ
   - ${gallbladderCondition}
   - Про белок: "Белок = жизнь". Если дефицит — объясни риски для мышц/иммунитета.

### 4. ✨ МОТИВАЦИЯ
   - Краткое напутствие.

=== ЗАПРЕТЫ ===
- НЕ ИСПОЛЬЗУЙ (А), (Б), [Скобки] в заголовках.
- НЕ ПИШИ СПИСКИ СЪЕДЕННОГО В НАЧАЛЕ БЛОКОВ.
- НЕ БУДЬ "ПРОДАВЦОМ". БУДЬ ЭКСПЕРТОМ.
`;
}

// --- 3. ОСНОВНОЙ ОБРАБОТЧИК ---

export default async function handler(req, res) {
    // 1. CORS
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

    if (req.method === 'OPTIONS') {
        res.status(200).end();
        return;
    }
    if (req.method !== 'POST') {
        return res.status(405).json({ error: 'Only POST' });
    }

    try {
        const apiKey = process.env.GEMINI_API_KEY;
        if (!apiKey) throw new Error('Нет GEMINI_API_KEY');

        const userData = req.body;
        if (!userData?.dailyFact || !userData?.goal) {
            return res.status(400).json({ error: 'Нет данных' });
        }

        // 2. Данные
        const goalTitle = getGoalTitle(userData.goal);
        
        const mandatoryOpening = `👋 **Приветствую! На связи Андрей Солдатенко.**\n\nЯ проанализировал Ваш рацион с учетом Вашей цели: **${goalTitle}**.\nДавайте посмотрим, как Ваш организм реагирует на то топливо, которое Вы ему даете 🔋.`;
        
        const disclaimerBlock = `\n\n------------------\n⚠️ **ВАЖНОЕ ПРИМЕЧАНИЕ:**\nДанные рекомендации основаны на анализе предоставленных Вами цифр и моем профессиональном опыте. Они носят информационный характер.\nВы принимаете на себя полную ответственность за использование этой информации.\nЕсли у Вас имеются хронические заболевания или Вы принимаете лекарственные препараты, перед любыми изменениями в рационе обязательна консультация с Вашим лечащим врачом.`;

        // 3. Вызов AI
        const analysisPrompt = buildAnalysisPrompt(userData);
        const genAI = new GoogleGenerativeAI(apiKey);
        const model = genAI.getGenerativeModel({ model: "gemini-2.5-flash" });

        const aiText = await callGeminiWithRetry(model, analysisPrompt, 6);
        
        const fullText = mandatoryOpening + "\n\n" + aiText.trim() + disclaimerBlock;

        res.status(200).json({ text: fullText });

    } catch (error) {
        console.error('Error:', error.message);
        res.status(500).json({ error: 'Generation failed' });
    }
}
