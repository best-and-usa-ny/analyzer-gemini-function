import { GoogleGenerativeAI } from '@google/generative-ai';

export const config = {
    api: { bodyParser: true },
};

// --- 1. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ---

const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));

async function callGeminiWithRetry(model, prompt, retries = 3) {
    let delay = 2000;
    for (let i = 0; i < retries; i++) {
        try {
            const result = await model.generateContent(prompt);
            return result.response.text();
        } catch (err) {
            console.warn(`Gemini 503/Overloaded. Попытка ${i + 1}/${retries}`);
            if (i === retries - 1) throw err;
            await sleep(delay);
            delay *= 1.5;
        }
    }
}

function formatMealsForAnalysis(meals) {
    if (!meals || typeof meals !== 'object') return 'Данные о приемах пищи отсутствуют (голод).';
    
    const keys = Object.keys(meals);
    if (keys.length === 0) return 'Приемы пищи не записаны.';

    let text = '';
    const order = ['breakfast', 'snack1', 'lunch', 'snack2', 'dinner', 'snack3'];
    
    const mealKeys = keys.sort((a, b) => {
        return order.indexOf(a) - order.indexOf(b);
    });

    mealKeys.forEach(key => {
        const m = meals[key];
        if (m) {
            text += `[${m.name || 'Прием пищи'}]:\n`;
            if (m.items && Array.isArray(m.items) && m.items.length > 0) {
                m.items.forEach(item => {
                    text += ` - ${item.name} (${item.grams}г): Ккал ${item.calories}, Б ${item.proteins}, Ж ${item.fats}, У ${item.carbs}\n`;
                });
            } else {
                text += ` - (Пусто/Пропущено)\n`;
            }
        }
    });
    return text;
}

function getGoalTitle(goalCode) {
    const goals = {
        'lose': 'Снижение веса 📉',
        'maintain': 'Поддержание веса и здоровья ⚖️',
        'gain': 'Набор мышечной массы 💪',
        'diabetes1': 'Контроль диабета 1 типа 🩸',
        'diabetes2': 'Контроль диабета 2 типа / ИР 🩸',
        'no_gallbladder': 'Питание при удаленном желчном 🚑',
        'gallstones': 'Питание при ЖКБ (камни) 💎',
        'gi_issues': 'Восстановление ЖКТ 🌿',
        'app': 'Антипаразитарная программа 🚫',
        'anticandida': 'Антикандидный протокол 🍄'
    };
    return goals[goalCode] || 'Снижение веса';
}

// --- 2. ГЕНЕРАЦИЯ ПРОМПТА (ВЕРСИЯ 9.0 - LUNCH FIX) ---
function buildAnalysisPrompt(userData) {
    const { goal, dailyFact, dailyTarget, meals } = userData;
    const goalTitle = getGoalTitle(goal);
    const mealsData = formatMealsForAnalysis(meals);

    const gallbladderCondition = (goal === 'no_gallbladder' || goal === 'gallstones') 
        ? "🚑 **КРИТИЧНО:** У клиента проблемы с желчным. Настаивай на **дробном питании (5-6 раз)**. Пропуски еды опасны." 
        : "Напомни про важность питания **4-5 раз в день** для разгона метаболизма.";

    return `
Ты — Андрей Солдатенко, опытный нутрициолог (20 лет стажа) и основатель Best&People.
Твой стиль: Экспертный, заботливый, живой. Ты не робот, ты наставник.
Твой инструмент: Markdown (жирный текст, списки).

=== ВХОДНЫЕ ДАННЫЕ ===
ЦЕЛЬ: ${goalTitle}
ФАКТ: Ккал ${dailyFact.calories} (Цель ${dailyTarget.calories}), Белки ${dailyFact.proteins} (Цель ${dailyTarget.proteins}), Вода ${dailyFact.water} мл (Цель ${dailyTarget.water}).
МЕНЮ КЛИЕНТА:
${mealsData}

=== БАЗА ЗНАНИЙ ДЛЯ РЕКОМЕНДАЦИЙ ===
1. **ИДЕАЛЬНЫЙ ЗАВТРАК (3 ШАГА):** Алоэ (Очищение) -> Shape/Detox (Питание) -> Чай (Энергия).
2. **ОБЕД:** Это ФУНДАМЕНТ (45% рациона). Это не перекус. Это Первое+Второе или Shape Smart Food (полная порция).
3. **ЗОЛОТОЙ ЧАС (16:00):** Лучшее время для Detox Gentle.
4. **УЖИН:** Легкий белок + клетчатка.

=== ТВОЯ ЛОГИКА АНАЛИЗА (СТРОГО СЛЕДУЙ ЕЙ) ===

1. **ВОДА:**
   - Чистая вода = Идеал 💧.
   - Функциональные напитки (Алоэ, Drain) = Хорошо ✅.
   - Кофе/Алкоголь = Обезвоживание 🛑.

2. **РАЗБОР ПРИЕМОВ ПИЩИ (СТРОГИЕ СЦЕНАРИИ):**

   **☀️ ЗАВТРАК:**
   - Если обычная еда: Похвали, но предложи "Идеальный Завтрак" как более легкую альтернативу.
   - Если B&P: Проверь наличие 3-х шагов.

   **🥘 ОБЕД (КЛЮЧЕВОЙ МОМЕНТ):**
   - **Если ПРОПУСК:** - 🛑 Жестко укажи: "Обед — это 45% всей энергии дня. Орешками тут не отделаешься."
     - **Проблема:** Спроси, неудобно ли носить контейнеры/судки с едой на работу/учебу?
     - **Решение:** "Если нет времени на 'Первое-Второе-Третье', возьмите Shape Smart Food или Пюре. Это ПОЛНОЦЕННЫЙ ОБЕД в одном стакане, а не перекус."
   - **Если поел:** Оцени состав (Белок + Сложные углеводы + Овощи).

   **⏰ ПОЛДНИК (16:00):**
   - Здесь (и только здесь) уместны перекусы (орехи/фрукты), но лучше всего — **Detox Gentle**.
   - Если пропуск: Предупреди о вечернем жоре.

   **🌙 УЖИН:**
   - Если пропуск: Предложи быстрый B&P.
   - **Вариативность:** Не повторяй продукты (если в обед мясо — тут рыба).

3. **МЕТОД ДВУХ ПУТЕЙ (Обязательно на Ужин или Обед):**
   - 🐢 **Путь 1 (Обычная еда):** КОНКРЕТНЫЙ рецепт с граммами (напр: "140г филе индейки + гречка").
   - 🚀 **Путь 2 (Smart Food):** Предложи Shape / Пюре. Аргумент: "Без контейнеров, без готовки, 1 минута, полноценная еда".

=== СТРУКТУРА ОТВЕТА (СТРОГО MARKDOWN) ===

**ВАЖНО:**
- ИСПОЛЬЗУЙ \`###\` ДЛЯ ГЛАВНЫХ БЛОКОВ.
- ИСПОЛЬЗУЙ **ЖИРНЫЙ ШРИФТ** ДЛЯ НАЗВАНИЙ ПРИЕМОВ ПИЩИ.

### 1. 💧 ВОДНЫЙ БАЛАНС
   - Анализ.

### 2. 🍽️ ПОШАГОВЫЙ РАЗБОР РАЦИОНА
   
   **☀️ ЗАВТРАК**
   - Анализ.
   
   **🍏 ВТОРОЙ ЗАВТРАК**
   - Анализ.
   
   **🥘 ОБЕД**
   - Анализ (Фундамент 45%!).
   
   **⏰ ПОЛДНИК: "ЗОЛОТОЙ ЧАС" (16:00)**
   - Анализ + Detox Gentle.

   **🌙 УЖИН**
   - Анализ.
   - **ПРИМЕНИ МЕТОД ДВУХ ПУТЕЙ:**
     - 🐢 Обычная еда (Рецепт).
     - 🚀 Smart Food (B&P).

### 3. 🧬 ФИЗИОЛОГИЯ
   - ${gallbladderCondition}
   - Про белок.

### 4. ✨ МОТИВАЦИЯ
   - Кратко.

=== ЗАПРЕТЫ ===
- ЗАПРЕЩЕНО использовать \`####\`.
- ЗАПРЕЩЕНО предлагать перекус (орехи) ВМЕСТО Обеда. Обед должен быть полным.
`;
}

// --- 3. ОСНОВНОЙ ОБРАБОТЧИК ---

export default async function handler(req, res) {
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

    if (req.method === 'OPTIONS') {
        res.status(200).end();
        return;
    }

    try {
        if (req.method !== 'POST') {
            return res.status(405).json({ error: 'Only POST' });
        }

        const apiKey = process.env.GEMINI_API_KEY;
        if (!apiKey) throw new Error('Нет GEMINI_API_KEY');

        const userData = req.body;
        if (!userData || !userData.dailyFact || !userData.goal) {
            return res.status(400).json({ error: 'Нет данных или некорректный формат' });
        }

        const goalTitle = getGoalTitle(userData.goal);
        
        const mandatoryOpening = `👋 **Приветствую! На связи Андрей Солдатенко.**\n\nЯ проанализировал Ваш рацион с учетом Вашей цели: **${goalTitle}**.\nДавайте посмотрим, как Ваш организм реагирует на то топливо, которое Вы ему даете 🔋.`;
        
        const disclaimerBlock = `\n\n------------------\n⚠️ **ВАЖНОЕ ПРИМЕЧАНИЕ:**\nДанные рекомендации основаны на анализе предоставленных Вами цифр и моем профессиональном опыте. Они носят информационный характер.\nВы принимаете на себя полную ответственность за использование этой информации.\nЕсли у Вас имеются хронические заболевания или Вы принимаете лекарственные препараты, перед любыми изменениями в рационе обязательна консультация с Вашим лечащим врачом.`;

        const analysisPrompt = buildAnalysisPrompt(userData);
        const genAI = new GoogleGenerativeAI(apiKey);
        const model = genAI.getGenerativeModel({ model: "gemini-2.5-flash" });

        const aiText = await callGeminiWithRetry(model, analysisPrompt, 6);
        
        const fullText = mandatoryOpening + "\n\n" + aiText.trim() + disclaimerBlock;

        res.status(200).json({ text: fullText });

    } catch (error) {
        console.error('Server Error:', error.message);
        res.status(500).json({ error: 'Ошибка генерации совета.' });
    }
}
